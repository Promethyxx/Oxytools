[package]
name = "vlc_spy"
version = "0.1.0"
edition = "2021"

[dependencies]
# Pour les requêtes HTTP vers VLC
reqwest = { version = "0.13", features = ["blocking", "json"] }
# Pour manipuler le format JSON
serde_json = "1.0"
# Pour l'horodatage des logs
chrono = "0.4"